ReactTalk
=========

A bad introduction to React.js

To play with the code:

* install Node.js
* Clone this repo
* On the command line, go to the repo's folder
* run:

```bash
  npm install
  gulp clean && gulp
```
